"""bastion setup — topology detection, package abstraction, and the wizard (Phase 5).

The AI-assisted setup path (sanitized prompts, §10 step 3) is Phase 6 and lives in a
separate `ai_assistant.py`; nothing here makes a network/API call.
"""
